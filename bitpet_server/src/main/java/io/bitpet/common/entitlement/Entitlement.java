package io.bitpet.common.entitlement;

/**
 * 유료 티어로 갈릴 수 있는 기능 단위.
 *
 * <p><b>지금은 어디에도 적용되어 있지 않다.</b> 상수를 미리 늘어놓지 않는 것도 같은 이유다 —
 * 무엇이 유료가 될지는 아직 정해지지 않았고, 추측으로 만든 상수는 지우기보다 남겨두게 된다.
 * 실제로 티어를 나눌 때 그 기능의 이름으로 여기에 추가한다.
 */
public enum Entitlement {
    /**
     * 자리를 비워둘 수 없어 두는 값. 어떤 기능도 이걸로 판정하지 않는다.
     * 첫 유료 기능이 정해지면 이 상수는 지운다.
     */
    NONE
}
